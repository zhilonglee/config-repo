server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhilonglee/config-repo      # 公用
          search-paths:
            - foo   # foo路径
            - bar   # bar路径
