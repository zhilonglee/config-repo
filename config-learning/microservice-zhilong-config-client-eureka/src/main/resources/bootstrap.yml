spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: microservice-zhilong-config-server-eureka
  application:
    name: foobar
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin@localhost:8761/eureka
  instance:
    prefer-ip-address: true
    